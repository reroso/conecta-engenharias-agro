<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title || 'Conecta Engenharias Agro' %></title>
    
    <!-- CSS -->
    <link rel="stylesheet" href="/css/style.css">
    
    <!-- Chart.js - Versão compatível sem módulos ES6 -->
    <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
    
    <!-- Favicon -->
    <link rel="icon" type="image/png" href="/favicon.png">
</head>
<body>
    <!-- Header -->
    <header class="header">
        <nav class="navbar">
            <a href="/" class="logo">
                🌱 Conecta Engenharias Agro
            </a>
            
            <% if (user) { %>
            <ul class="nav-links">
                <li><a href="/">Dashboard</a></li>
                <li><a href="/plantacao">Plantações</a></li>
                <li><a href="/clima">Clima</a></li>
                <li><a href="/recomendacao">Recomendações</a></li>
                
                <li class="user-menu">
                    <a href="#" class="user-toggle">
                        👤 <%= user.nome %>
                    </a>
                    <div class="user-dropdown">
                        <a href="/auth/profile">Perfil</a>
                        <a href="/plantacao/nova">Nova Plantação</a>
                        <div style="border-top: 1px solid #dee2e6; margin: 0.5rem 0;"></div>
                        <form action="/auth/logout" method="POST" style="margin: 0;">
                            <button type="submit" style="background: none; border: none; width: 100%; text-align: left; padding: 0.75rem 1rem; cursor: pointer; color: #dc3545;">
                                Sair
                            </button>
                        </form>
                    </div>
                </li>
            </ul>
            <% } %>
        </nav>
    </header>

    <!-- Main Content -->
    <main class="main-content">
        <!-- Flash Messages -->
        <% if (typeof success !== 'undefined' && success) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
        <% } %>
        
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
        
        <% if (typeof warning !== 'undefined' && warning) { %>
            <div class="alert alert-warning">
                <%= warning %>
            </div>
        <% } %>
        
        <% if (typeof info !== 'undefined' && info) { %>
            <div class="alert alert-info">
                <%= info %>
            </div>
        <% } %>

        <!-- Page Content -->
        <%- body %>
    </main>

    <!-- Footer -->
    <footer style="background: #f8f9fa; padding: 2rem 0; margin-top: 3rem; text-align: center; color: #6c757d;">
        <div style="max-width: 1200px; margin: 0 auto;">
            <p>&copy; 2024 Conecta Engenharias Agro. Desenvolvido para integração de dados climáticos e recomendações agrícolas.</p>
            <p style="font-size: 0.9rem; margin-top: 0.5rem;">
                Dados climáticos fornecidos pelo INMET | 
                <a href="https://portal.inmet.gov.br" target="_blank" style="color: #2e8b57;">Instituto Nacional de Meteorologia</a>
            </p>
        </div>
    </footer>

    <!-- JavaScript -->
    <script src="/js/app.js"></script>
    
    <!-- Additional scripts -->
    <%- typeof scripts !== 'undefined' ? scripts : '' %>
</body>
</html>